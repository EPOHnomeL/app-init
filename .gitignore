# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js / build
/.next/
/out/
/build
next-env.d.ts

# env
.env
.env*.local
.env.production

# database / backend local
/.convex
.supabase/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel / turbo
.vercel
.turbo

# editor / OS
.idea/
*.swp

# claude — keep settings.json + bundled skills tracked; ignore local overrides + state
.claude/settings.local.json
.claude/state/
.claude/.cache/

# knowledge bank build artifacts (generated SPA bundle, kept out of VCS by default)
/docs/.generated/
